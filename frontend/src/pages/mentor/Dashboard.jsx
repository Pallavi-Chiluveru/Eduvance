import { useEffect,useState } from 'react';import { mentorAPI } from '../../services/apiService';import LoadingSpinner from '../../components/common/LoadingSpinner';import StatCard from '../../components/common/StatCard';import { HiOutlineUsers,HiOutlineLightningBolt,HiOutlineBookOpen,HiOutlineChatAlt2,HiOutlineCalendar } from 'react-icons/hi';
export default function MentorDashboard(){const[d,setD]=useState(null),[e,setE]=useState('');const load=()=>mentorAPI.getDashboard().then(r=>setD(r.data.data)).catch(x=>setE(x.response?.data?.message||'Unable to load dashboard'));useEffect(load,[]);if(!d&&!e)return <LoadingSpinner/>;if(e)return <p role="alert">{e} <button onClick={load} className="underline">Try again</button></p>;const s=d.stats;return <div className="space-y-6"><header><h1 className="text-2xl font-bold">Mentor Dashboard</h1><p style={{color:'var(--text-muted)'}}>Guide assigned learners using their real learning progress.</p></header><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-5"><StatCard title="Assigned Learners" value={s.assignedLearners} icon={HiOutlineUsers} color="indigo"/><StatCard title="Active Learners" value={s.activeLearners} icon={HiOutlineLightningBolt} color="emerald"/><StatCard title="Courses In Progress" value={s.coursesInProgress} icon={HiOutlineBookOpen} color="cyan"/><StatCard title="Pending Feedback" value={s.pendingFeedback} icon={HiOutlineChatAlt2} color="amber"/><StatCard title="Sessions" value={s.mentoringSessions} icon={HiOutlineCalendar} color="rose"/></div></div>}
