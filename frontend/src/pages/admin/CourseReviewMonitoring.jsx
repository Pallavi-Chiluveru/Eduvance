import{useEffect,useState}from'react';import{adminAPI}from'../../services/apiService';import LoadingSpinner from'../../components/common/LoadingSpinner';
export default function CourseReviewMonitoring(){const[x,setX]=useState(null),[filter,setFilter]=useState('');useEffect(()=>{adminAPI.getCourseReviewOverview().then(r=>setX(r.data.data.courses))},[]);if(!x)return <LoadingSpinner/>;const list=filter?x.filter(c=>c.status===filter):x;return <div className="space-y-6"><header><h1 className="text-2xl font-bold">Course Review Monitoring</h1><p style={{color:'var(--text-muted)'}}>Platform-level oversight of the content-review pipeline.</p></header><select value={filter} onChange={e=>setFilter(e.target.value)} className="rounded-lg border bg-transparent p-2"><option value="">All statuses</option>{['draft','pending_review','under_review','changes_requested','approved','rejected','published'].map(v=><option key={v} value={v}>{v.replace('_',' ')}</option>)}</select><div className="overflow-x-auto rounded-xl border" style={{borderColor:'var(--border-color)'}}><table className="w-full text-sm"><thead style={{background:'var(--bg-tertiary)'}}><tr>{['Course','Instructor','Status','Reviewer','Updated'].map(h=><th className="p-3 text-left" key={h}>{h}</th>)}</tr></thead><tbody>{list.map(c=><tr key={c._id} className="border-t" style={{borderColor:'var(--border-color)'}}><td className="p-3 font-medium">{c.name}</td><td className="p-3">{c.instructor?.firstName} {c.instructor?.lastName}</td><td className="p-3 capitalize">{c.status.replace('_',' ')}</td><td className="p-3">{c.reviewedBy?`${c.reviewedBy.firstName} ${c.reviewedBy.lastName}`:'—'}</td><td className="p-3">{new Date(c.updatedAt).toLocaleDateString()}</td></tr>)}</tbody></table></div></div>}
