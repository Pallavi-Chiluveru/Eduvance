import { useEffect, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { FaMoon, FaSun } from 'react-icons/fa';
import Logo from '../common/Logo';
import { useAuth } from '../../hooks/useAuth';
export default function VerificationLayout({children}){const {user,logout}=useAuth();const navigate=useNavigate();const [dark,setDark]=useState(()=>localStorage.getItem('theme')!=='light');useEffect(()=>{document.documentElement.classList.toggle('dark',dark);localStorage.setItem('theme',dark?'dark':'light')},[dark]);const signOut=async()=>{await logout();navigate('/login',{replace:true})};return <div className="min-h-screen bg-slate-50 dark:bg-slate-950 text-slate-900 dark:text-white"><header className="h-16 px-4 sm:px-8 flex items-center justify-between border-b border-slate-200 dark:border-slate-800 bg-white/85 dark:bg-slate-900/85 backdrop-blur-xl"><Logo size="md" variant={dark?'light':'dark'}/><div className="flex items-center gap-3"><div className="hidden sm:block text-right"><p className="text-sm font-semibold">{user?.firstName} {user?.lastName}</p><p className="text-xs text-slate-500">Instructor applicant</p></div><button onClick={()=>setDark(v=>!v)} className="p-2.5 rounded-xl border border-slate-200 dark:border-slate-700" aria-label="Toggle theme">{dark?<FaSun/>:<FaMoon/>}</button><button onClick={signOut} className="px-3 py-2 rounded-xl border border-slate-200 dark:border-slate-700 text-sm font-semibold">Logout</button></div></header><main className="p-4 sm:p-8">{children}</main></div>}
